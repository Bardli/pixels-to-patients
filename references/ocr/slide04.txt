EXPERIMENTAL SETUP
A 32x32 toy task, a 3-stage CNN, and
deliberatel t d classifi
a deliberately-not-converged Classifier.
A bright 5x5 cross on a noisy background. Class 0 if the cross sits in the left FRAG om. GEST
half, class 1 if right. A handful of dim 3x3 distractors per scene are added as red
herrings. 3000 400 200
Ground-truth importance is the 7x7 window around the target cross — the only region a
faithful attribution should highlight.
input stagel stage2 x stage3 fc
1+ 32? > 8: 167 > 16 - 8? > 32+ 1? > 32
664 params 3,488 params 13,888 params 66 params
x stage2 output (16 x 8 x 8) is the tap point for every CAM-family method — last block with usable spatial extent before stage3's global
average pool collapses everything to (32, 1, 1).

OPTIMIZER EARLY STOP WHY NOT 100 %?

opt Adam Stop as soonas val_ acc = 0.95 — typically epoch 2. At full convergence the logit gap blows past 15, gradients

lr 2e-3 Test accuracy ends near 1.0 either way. oy/0A go numerically asymmetric, and Grad-CAM

wd 1e-3 collapses. Saturation is the artefact |GC was proposed to

batch 64 fix — we want to see it on the trained-not-saturated

5 regime.

loss  cross-entropy
04 SETUP
