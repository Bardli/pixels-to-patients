FAILURE MODE - GRAD-CAM
When most Q go negative,
ReLU eats the whole heatmap.
Why a single-summary-weight method is fragile under high-confidence
predictions — and why per-position methods (LayerCAM, IGC) don't have lage chontel a valucy ase negative te wetdhtcd cus non peste ceelyurere; Set) zeroes i)
ik pratt Per-position metnads like LayerCAM avoid this because they don't average gradients across space
«k= mean, dim=spatial)
01 Model is confident. Spatial averaging produces 6 positive, 10 Tkaa Rett...
inowt min-0.32 max~0.0000 upsample = overla

negative aj for this input. class 1 target max=-0.02 ~ everything killed! Perempy) Gt for reference
02 Vy a: A\ is non-positive almost everywhere — min -0.32 , max ea . | eal

-0.02. L+}
03 ReLU clips it to zero. The heatmap collapses to a blank field.
04 LayerCAM avoids it: ReLU acts on per-position gradients before the

sum. Each location keeps its own sign.
07 FAILURE MODE
